
//IMGUI RENDER


// goto:#DEBUGBOX
// goto:#Options
// goto:#ItemRender
// goto:#Monster
// 
// goto:#Inventory
// goto:#ADDITEM
// goto:#PlayerStatsDebug
//goto:#LoadMenu

//

/*code snippet// print random modifiers

            for (int i = 0; i < 100; i++)
            {
				int rnd = dist(mt);

                for (auto i : tmpMods) {
                    if (rnd < i->weight)
                    {
                        printf("Random mod:%s  %d rand nr:%i\n",i->GetName().c_str(),i->weight,rnd);
                        break;
                    }
                    
                    rnd -= i->weight;
            }
            }
*/